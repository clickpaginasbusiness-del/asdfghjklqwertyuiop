import { Clock } from 'lucide-react'
import { DemoPlaceholderPage } from '../DemoPlaceholderPage'

export default function HorariosDemoPage() {
  return (
    <DemoPlaceholderPage
      titulo="Horários"
      icon={Clock}
      descricao="No plano real, você configura os dias e horários de funcionamento do seu estabelecimento."
    />
  )
}
